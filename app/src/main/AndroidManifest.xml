<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.canteenautomation">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.CanteenCustomerOrdersActivity"></activity>
        <activity android:name=".Activities.CanteenWithdrawActivity" />
        <activity android:name=".Activities.CanteenDepositActivity" />
        <activity android:name=".Activities.WithdrawActivity" />
        <activity android:name=".Activities.DepositeAmountActivity" />
        <activity android:name=".Activities.ViewOrdersActivity" />
        <activity android:name=".Activities.ViewFoodsMenueDetailsActivity" />
        <activity android:name=".Activities.ViewMenuActivity" />
        <activity android:name=".Activities.ViewFoodDetailsActivity" />
        <activity android:name=".Activities.ViewFoodActivity" />
        <activity android:name=".Activities.AddNewFoodActivity" />
        <activity android:name=".Activities.ViewCanteenDetailsActivity" />
        <activity android:name=".Activities.ViewCanteenActivity" />
        <activity android:name=".Activities.ViewStudentDetailsActivity" />
        <activity android:name=".Activities.ViewStudentActivity" />
        <activity android:name=".Activities.SplashActivity" />
        <activity android:name=".Activities.AddNewStudentActivity" />
        <activity android:name=".Activities.StudentHomeActivity" />
        <activity android:name=".Activities.CanteenHomeActivity" />
        <activity android:name=".Activities.AddNewCanteenActivity" />
        <activity android:name=".Activities.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.MainActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>